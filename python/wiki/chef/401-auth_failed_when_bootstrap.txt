* slow workaround
EDITOR="vi" knife client create $1 -f /tmp/$1.pem
scp /tmp/$1.pem root@$1:/etc/chef/client.pem

* comment from chip@frnd
kerute: Sometimes people set the hw clock to
non-gmt and don't use tzdata to adjust to the
local time zone.
